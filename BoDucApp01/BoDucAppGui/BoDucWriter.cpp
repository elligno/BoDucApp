#include "BoDucWriter.h"

namespace bdGui 
{
	void BoDucWriter::createReport(const std::string & aBonDeLivraison)
	{
		//  for the current version we write the result in a .txt file.
		// Default file name is "BonDeLivraison" but it should contains
		// the date (similar to the DamBreak++ Simulator where the date
		// is append at the end of the name).
	}
} // End of namespace

